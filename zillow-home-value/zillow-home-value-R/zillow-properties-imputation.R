library(missMDA)
library(FactoMineR)

############################### Data Imputation - Properties ################################################

# Apply factor analysis for imputation
# properties <- imputeFAMD(data.frame(properties), ncp=5, verbose=TRUE)
# famdval <- PCA(properties,  ncp = 20)







